# 《星野栖所》技术架构优化方案

## 一、技术选型优化

### 1.1 渲染引擎
- **主渲染引擎**: Three.js r160+ (WebGL 2.0)
- **物理引擎**: Cannon.js / Ammo.js (地形交互、碰撞检测)
- **后处理**: Three.js EffectComposer (Bloom、泛光、景深)

### 1.2 性能优化方案
- **场景管理**: 分片加载 (Chunk-based Loading)
  - 浮空岛屿按需求加载，降低设备功耗
  - LOD (Level of Detail) 多级细节模型
  - 视锥剔除 (Frustum Culling) + 遮挡剔除 (Occlusion Culling)
- **资源管理**: 
  - GLTF/GLB 压缩格式 (Draco 压缩)
  - 纹理图集 (Texture Atlas)
  - 实例化渲染 (InstancedMesh) 用于草地、树木

### 1.3 架构模式
- **ECS (Entity Component System)**: 适用于复杂游戏对象管理
- **状态机**: AI 行为、UI 状态管理
- **观察者模式**: 事件系统解耦

## 二、核心系统数据结构优化

### 2.1 时间系统 (Time System)
```typescript
class GameTimeSystem {
  // 游戏内时间流速配置
  timeScale: {
    gameHourToRealMinutes: 15,  // 1游戏小时 = 15现实分钟
    dayNightCycle: 24,          // 完整昼夜循环
    seasonalCycle: 12,           // 四季周期(天)
  }

  // 昼夜阶段定义
  dayPhase: {
    DAWN: { start: 5, end: 7 },      // 清晨
    NOON: { start: 11, end: 14 },     // 正午
    DUSK: { start: 17, end: 20 },    // 黄昏
    NIGHT: { start: 21, end: 4 }      // 深夜
  }

  // 季节枚举
  season: ['SPRING', 'SUMMER', 'AUTUMN', 'WINTER']

  // 天气系统
  weather: [
    { type: 'CLEAR', probability: 0.30 },      // 晴
    { type: 'FOG', probability: 0.20 },         // 雾
    { type: 'RAIN', probability: 0.20 },        // 细雨
    { type: 'STAR_RAIN', probability: 0.10 },    // 星雨(稀有)
    { type: 'SNOW', probability: 0.15 },         // 落雪
    { type: 'AURORA', probability: 0.05 }       // 极光(稀有)
  ]
}
```

### 2.2 作物生长系统 (Crop Growth System)
```typescript
interface CropData {
  id: string;
  name: string;
  type: 'BASIC' | 'STAR_PLANT';
  growthTime: number;        // 生长时长(分钟)
  seasons: Season[];         // 可种植季节
  weatherRequirement: Weather[]; // 天气要求
  harvestYield: ItemQuantity[];
  adjacentBonus: AdjacentBonus[];
}

interface AdjacentBonus {
  neighborType: CropType;
  bonusType: 'YIELD' | 'GROWTH_TIME' | 'RARE_DROP';
  bonusValue: number;
}

// 共生种植配置示例
const companionPlantingRules: AdjacentBonus[] = [
  { typeA: 'HERB', typeB: 'FLOWER', bonus: { type: 'YIELD', value: 0.20 } },
  { typeA: 'FRUIT_TREE', typeB: 'VINE', bonus: { type: 'GROWTH_TIME', value: -0.15 } },
  { typeA: 'STAR_PLANT', typeB: 'STAR_PLANT', bonus: { type: 'RARE_DROP', value: 0.10 } }
];
```

### 2.3 异兽AI系统 (Beast AI System)

#### 2.3.1 AI行为树设计 (Behavior Tree)
```typescript
// 基础AI行为节点
abstract class AINode {
  execute(beast: BeastEntity): NodeStatus;
}

class SequenceNode extends AINode {
  children: AINode[];
  // 顺序执行，所有子节点成功才返回成功
}

class SelectorNode extends AINode {
  children: AINode[];
  // 选择执行，第一个成功即返回成功
}

class ParallelNode extends AINode {
  children: AINode[];
  // 并行执行所有子节点
}

// 异兽基础行为
class BeastBehaviorTree {
  root: AINode;

  // 通用行为节点
  patrolBehavior(): AINode;      // 巡逻行为
  collectBehavior(): AINode;    // 采集行为
  interactBehavior(): AINode;    // 互动行为
  idleBehavior(): AINode;        // 待机行为
}
```

#### 2.3.2 各异兽AI行为定义
```typescript
// 星绒兔 AI
class StarBunnyAI extends BeastBehaviorTree {
  buildTree(): void {
    this.root = new SelectorNode([
      new SequenceNode([
        new CheckCropReadyNode(),
        new MoveToTargetNode(),
        new HarvestCropNode(),
        new ReturnHomeNode()
      ]),
      new SequenceNode([
        new CheckIntimacyThreshold(500),
        new UnlockAutoPlantNode()
      ]),
      this.patrolBehavior()
    ]);
  }
}

// 云羽雀 AI
class CloudBirdAI extends BeastBehaviorTree {
  buildTree(): void {
    this.root = new SelectorNode([
      new SequenceNode([
        new ScanSkyResourcesNode(),
        new FlyToTargetNode(),
        new CollectFloatMaterialNode()
      ]),
      this.aerialPatrolBehavior()
    ]);
  }
}
```

### 2.4 地图场景配置 (Map Configuration)
```typescript
interface SceneConfig {
  id: string;
  name: string;
  unlockLevel: number;
  resources: ResourceSpawn[];
  beasts: BeastSpawn[];
  scenery: SceneryObject[];
  lighting: LightingConfig;
}

// 资源配置表
interface ResourceSpawn {
  resourceId: string;
  spawnType: 'NORMAL' | 'RARE' | 'WEATHER_SPECIFIC';
  refreshTime: number;      // 刷新时间(分钟)
  weatherCondition?: Weather;
  position: Vector3;
  respawnRadius: number;
}
```

## 三、生态联动系统 (Ecosystem System)

### 3.1 天气-季节-资源联动
```typescript
class EcosystemLinker {
  // 天气对作物影响
  weatherEffects: Map<Weather, CropEffect[]>;

  // 季节对资源刷新影响
  seasonalResources: Map<Season, ResourcePool>;

  // 联动规则配置表
  ecosystemRules: EcosystemRule[];

  applyWeatherEffect(crop: CropEntity, weather: Weather): void {
    const effects = this.weatherEffects.get(weather);
    effects?.forEach(effect => {
      crop.applyEffect(effect.type, effect.value);
    });
  }

  checkResourceSpawn(scene: SceneEntity): void {
    // 检查资源刷新条件
  }
}
```

### 3.2 异兽行为环境响应
```typescript
class BeastEnvironmentResponse {
  respondToWeather(beast: BeastEntity, weather: Weather): void {
    // 不同天气下异兽行为变化
    switch(weather) {
      case 'RAIN':
        beast.seekShelter();
        break;
      case 'SNOW':
        beast.increaseActivity();
        break;
    }
  }

  respondToSeason(beast: BeastEntity, season: Season): void {
    // 季节性行为变化
  }
}
```

## 四、性能优化架构

### 4.1 场景分片加载策略
```typescript
class ChunkLoader {
  // 分片大小配置
  chunkSize: { width: 100, height: 100, depth: 50 };

  // 加载优先级
  loadingPriority: {
    VISIBLE_CHUNKS: 1,      // 可见区域优先
    ADJACENT_CHUNKS: 2,     // 相邻区块次之
    PREDICTIVE_CHUNKS: 3    // 预测加载最低
  };

  // LOD 级别
  lodLevels: [
    { distance: 0, detail: 'HIGH' },
    { distance: 50, detail: 'MEDIUM' },
    { distance: 100, detail: 'LOW' }
  ];

  loadChunk(chunkId: string): Promise<ChunkData>;
  unloadChunk(chunkId: string): void;
}
```

### 4.2 渲染优化策略
```typescript
class RenderOptimizer {
  // 视锥剔除
  enableFrustumCulling(): void;

  // 实例化渲染 (草地、树木)
  useInstancedMesh(geometry: Geometry, count: number): InstancedMesh;

  // 纹理压缩
  compressTextures(): void;

  // 延迟渲染优化
  useDeferredShading(): void;
}
```

### 4.3 内存管理
```typescript
class MemoryManager {
  // 资源池管理
  resourcePool: Map<string, Pool<ResourceEntity>>;

  // 对象回收策略
  enableObjectPooling(): void;

  // 垃圾回收优化
  optimizeGarbageCollection(): void;

  // 内存监控
  monitorMemoryUsage(): MemoryStats;
}
```

## 五、数据持久化架构

### 5.1 本地存储方案
```typescript
class LocalDataManager {
  // 数据加密存储
  encryptData(data: GameSaveData): string;

  // 自动存档机制
  autoSave(): void;

  // 云端同步 (可选)
  cloudSync(): Promise<void>;

  // 数据版本管理
  versionControl(): void;
}
```

### 5.2 服务端校验 (防作弊)
```typescript
class ServerValidator {
  // 作物生长状态校验
  validateCropGrowth(saveData: GameSaveData): ValidationResult;

  // 资源获取合法性校验
  validateResourceCollection(action: GameAction): boolean;

  // 异兽行为校验
  validateBeastAction(action: BeastAction): boolean;

  // 离线收益计算
  calculateOfflineEarnings(saveData: GameSaveData): OfflineEarnings;
}
```

## 六、配置表系统 (Data-Driven Design)

### 6.1 Excel 配置导出
所有游戏逻辑配置使用 Excel 表格管理，通过工具导出为 JSON 供游戏读取：

- **作物配置表** (crops.xlsx)
- **异兽配置表** (beasts.xlsx)
- **场景配置表** (scenes.xlsx)
- **天气配置表** (weather.xlsx)
- **建造配方表** (building_recipes.xlsx)
- **剧情配置表** (story.xlsx)

### 6.2 热更新机制
```typescript
class HotfixManager {
  // 配置表热更新
  updateConfigTable(tableName: string, newData: any): void;

  // 资源热更新
  updateResource(bundleName: string, newBundle: AssetBundle): void;

  // 版本校验
  checkVersion(): Promise<VersionInfo>;
}
```

## 七、UI/UX 优化方案

### 7.1 极简透明UI规范
```typescript
class UIOptimization {
  // 透明窗口背景
  windowOpacity: 0.7;

  // 低遮挡设计
  minimizeUIOcclusion(): void;

  // 渐入渐出动画
  smoothTransitions(): void;

  // 无感交互
  gestureBasedControls(): void;
}
```

### 7.2 HUD 布局
- 顶部: 时间、天气、季节显示
- 左下: 角色/异兽状态
- 右下: 快捷工具栏
- 底部中央: 建造/种植工具切换
- 中央: 任务提示、剧情对话

## 八、开发优先级实现计划

### Phase 1: 核心框架 (2周)
1. 引擎初始化、场景管理基础
2. 时间系统、昼夜循环
3. 天气系统框架
4. 地图分片加载系统

### Phase 2: 核心玩法 (3周)
1. 作物种植系统
2. 共生种植逻辑
3. 地形编辑系统
4. 基础建造系统

### Phase 3: 特色系统 (3周)
1. 异兽AI行为树
2. 各异兽专属行为
3. 异兽交互系统
4. 亲密度系统

### Phase 4: 内容完善 (2周)
1. UI/UX 开发
2. 音效系统
3. 剧情系统
4. 社交基础功能

## 九、技术风险与应对

| 风险项 | 等级 | 应对方案 |
|-------|------|---------|
| 移动端性能 | 高 | LOD、实例化、帧率自适应 |
| 复杂AI性能 | 中 | 行为树节点缓存、优先级调度 |
| 配置表维护 | 中 | 自动化工具链、版本控制 |
| 数据同步 | 低 | 服务端校验、乐观锁机制 |

## 十、代码质量保障

### 10.1 编码规范
- TypeScript 严格模式
- ESLint + Prettier
- 组件化开发
- 完整的类型定义

### 10.2 测试策略
- 单元测试: Jest/Vitest
- 集成测试: Playwright (E2E)
- 性能测试: Lighthouse
- 兼容性测试: 多设备真机测试

---

**文档版本**: v1.0
**最后更新**: 2026-05-08
**制定人**: SOLO AI Assistant
**适用版本**: 一期首发版本
